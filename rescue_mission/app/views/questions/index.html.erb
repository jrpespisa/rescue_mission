<h1>Questions</h1>

<ol>
  <% @questions.each do |question|%>
    <li><%= link_to "#{question.title}", question_path(question) %></li>
  <% end %>
</ol>

<p><%= button_to "Submit Question", new_question_path, method: :get %></p>
